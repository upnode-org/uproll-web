# `uproll chain-info`

Retrieves files generated during deployment (e.g. `intent.toml`, `wallets.json`, genesis). Select a rollup, then choose the file to display.

```bash
uproll chain-info <project_name>
``` 