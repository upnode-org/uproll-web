# `uproll stop-rollup`

Stops a running rollup **and** deletes its project folder (because Kurtosis cannot restart an enclave once stopped). Use this when you are **finished** with a chain and need to reclaim resources.

```bash
uproll stop-rollup <project_name>
``` 