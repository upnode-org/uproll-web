# Stop Rollup

![stop](/docs/stop.gif)

Stops only the singular running rollup **and** deletes its project folder (because Kurtosis cannot restart an enclave once stopped). Use this when you are **finished** with a chain and need to reclaim resources.