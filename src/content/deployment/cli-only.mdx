import { Callout } from 'nextra/components'

# Deploy via CLI Only 🖥️

Run the interactive wizard:

```bash
uproll run
```

### Steps

1. Select **Deploy Rollup** from the menu.
2. Answer the prompts (chain ID, RPC URLs, gas params…).
3. Confirm to generate the config and automatically start deployment.

<Callout type="tip">Already have a YAML config? Run <code>uproll deploy -f path/to/config.yaml</code>.</Callout> 